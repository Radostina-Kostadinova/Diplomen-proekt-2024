@model TicketsHarbourApp.Models.Concert.ConcertDetailsVM

@{
    ViewData["Title"] = "Details";
}



<h1>Details</h1>

<div>
    <h4>ConcertDetailsVM</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.ConcertName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Performers)
        </dd>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Description)
        </dd>
       
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Picture)
        </dt>
        <dd class="col-sm-10">
            <img src="@Html.DisplayFor(model => model.Picture)" alt="Image" width="100" />
            @*@Html.DisplayFor(model => model.Picture)*@
        </dd>
    </dl>

</div>
<div>
    @if ((this.User.Identity.IsAuthenticated) && (this.User.IsInRole("Administrator")))
    {
        <a asp-action="Edit" asp-route-id="@Model.Id">Edit</a>
    }
    
    <a asp-action="Index">Back to List</a>
</div>
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
